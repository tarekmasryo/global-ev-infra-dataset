[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [
  ".venv",
  ".ruff_cache",
  "generated",
  "notebooks",
  "examples",
  "docs",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E203"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
